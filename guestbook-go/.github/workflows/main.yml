name: Go application

on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "imprithwish619" --password-stdin
        docker image build -t imprithwish619/go_application:latest /example_ridecell/guestbook-go/Dockerfile
        docker push imprithwish619/go_application:latest
